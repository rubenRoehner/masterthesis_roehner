\chapter{Implementation}\label{sec:implementation} % approx. 8 pages
The following chapter details the implementation of the proposed HRL framework, the evaluation process, and the baselines used for evaluation.

\section{Rebalancing Framework}\label{sec:implementation_rebalancing-framework}
The following sections detail the implementation of the Regional Distribution Coordinator, User-Incentive Coordinator, and the Demand Forecasting Module.
All these components are implemented using Python in the version \texttt{v3.10.6}.

\subsection{Regional Distribution Coordinator}\label{sec:implementation_rdc}
The Regional Distribution Coordinator (RDC) defines the RL agent of the first level of the proposed HRL framework (see \autoref{sec:design-rl_framework}).
The central component of the agent is the \texttt{RegionalDistributionCoordinator} class, which defines the agent itself.
The agent utilizes a \texttt{MultiHeadDQN} to choose rebalancing quotas, tackling inter-community imbalances in the system through manual rebalancing by service workers.
To ensure efficient learning and effective use of training data, the agent employs a Prioritized Experience Replay (PER) buffer, which is implemented in the \texttt{ReplayBuffer} class.
The experiences stored in this buffer are gained by interacting with the \texttt{EScooterRDCEnv}, which defines a custom environment based on the OpenAI Gym interface.

The configurable hyperparameters that influence the agent's ability to learn an efficient rebalancing strategy are defined in \autoref{tab:rdc_training_parameters}.


\begin{table}[!htb]
    \centering
        \begin{tabularx}{\textwidth}{lX}
        \toprule
        \textbf{Parameter} & \textbf{Description} \\
        \midrule
        \texttt{gamma} & The discount factor ($\gamma$) that determines the present value of future rewards, balancing immediate and long-term gains in the Bellman equation. \\
        \texttt{tau} & The interpolation factor ($\tau$) for the soft update of the target network, controlling how quickly the target network tracks the policy network. \\
        \midrule
        \texttt{state\_dim} & The dimensionality of the input state vector, determined by the number of geographic zones and the features used to describe each zone. \\
        \texttt{hidden\_size} & The number of neurons in each hidden layer of the Multi-Head Deep Q-Network, defining the model's learning capacity. \\
        \midrule
        \texttt{lr} & The initial learning rate ($\alpha_{lr}$) for the Adam optimizer, controlling the step size of gradient descent updates. \\
        \texttt{lr\_step\_size} & The number of training steps after which the learning rate is multiplicatively decayed. \\
        \texttt{lr\_gamma} & The multiplicative factor ($\gamma_{lr}$) by which the learning rate is decayed. \\
        \midrule
        \texttt{rb\_capacity} & The maximum number of transition experiences stored in the replay buffer. \\
        \texttt{batch\_size} & The number of experiences sampled from the replay buffer for each training step. \\
        \texttt{per\_alpha} & The prioritization exponent ($\alpha_{PER}$), controlling how aggressively priorities are used for sampling (0 = uniform sampling, 1 = full prioritization). \\
        \texttt{per\_beta\_start} & The initial value of the importance-sampling exponent ($\beta_{PER}$), which is annealed to 1.0 to correct for the bias introduced by prioritized sampling. \\
        \texttt{per\_beta\_frames} & The number of training frames over which the PER exponent $\beta$ is annealed to 1.0. \\
        \midrule
        \texttt{epsilon\_start} & The initial probability ($\epsilon_{start}$) of selecting a random action to encourage exploration. \\
        \texttt{epsilon\_end} & The minimum final probability ($\epsilon_{min}$) of selecting a random action, ensuring some exploration persists. \\
        \texttt{epsilon\_decay} & The multiplicative factor ($\delta_{\epsilon}$) used to gradually decrease the exploration rate from its initial value to its final value. \\
        \midrule
        \texttt{weight\_demand} & The weighting coefficient for the satisfied demand component ($w_{demand}^{RDC}$) in the composite reward function. \\
        \texttt{weight\_cost} & The weighting coefficient for the rebalancing cost penalty component ($w_{cost}^{RDC}$) in the composite reward function. \\
        \texttt{weight\_gini} & The weighting coefficient for the Gini index component ($w_{gini}^{RDC}$) in the composite reward function. \\
        \bottomrule
    \end{tabularx}
    \caption{Configurable parameters for the Regional Distribution Coordinator agent.}
    \label{tab:rdc_hyperparameters_description}
\end{table}

\subsubsection{Multi-Head Deep Q-Network Architecture (\texttt{MultiHeadDQN} Class)}

\subsubsection{State representation}
The state components, e.g., vehicle counts and forecasts, are stacked, resulting in a \texttt{torch.Tensor} of size $3, N_C$.


\subsubsection{Action space representation}

\subsubsection{Reward function representation}

\subsubsection{Environment (\texttt{EScooterRDCEnv} Class)}
The \texttt{EScooterRDCEnv} class implements the simulation environment used for training the RDC agent.
It follows the standard OpenAI Gym interface.
The \texttt{step()} function defines the core logic of the simulation, which is detailed in the following:

\begin{enumerate}
    \item \textbf{Action execution: }
        The \texttt{step()} method receives a vector as input.
        This vector contains the chosen rebalancing quotas ($A_t^{RDC}$) from the RDC agent.
        To ensure vehicle conservation, as defined in the rebalancing problem (\autoref{sec:design-problem_definition}), these quotas are scaled so that the sum of all quotas is zero, while maintaining the intended flow.
        
    \item \textbf{Rebalancing Simulation: }
        After scaling the rebalancing quotas, the rebalancing is performed with these scaled quotas.
        For communities with a positive quota, vehicles are added to the zones with the lowest vehicle counts first.
        Conversely, for communities receiving a negative quota, vehicles are removed from zones with the highest vehicle counts first.\todo{Ref: Simulation besser als Penalty um in den Constraints zu bleiben}
    \item \textbf{Demand Simulation: }
        After the vehicle counts are updated based on the rebalancing actions chosen by the agent, the user demand is simulated for the entire step (e.g., 60 minutes).
        Historical demand data drives this demand simulation.
        This historical demand data is slightly different than the demand forecasts, which are part of the agent's state.
        These differences come from forecasting errors by the Demand Forecasting Module.
        Historical demand is simulated sequentially, continuously updating vehicle counts across all zones.
    \item \textbf{Observation and Reward Calculation : }
        After both rebalancing and demand are simulated, the reward for the time step is calculated.
        As detailed in \autoref{sec:design-rl_framework-rdc}, the reward consists of the ratio between satisfied and unsatisfied demand, a penalty for the amount of vehicles rebalanced, and the Gini index to reward equally distributed vehicles.
        Then, a new state observation $S_{t+1}^{RDC}$ is created based on the updated vehicle counts and newly forecasted demand.
        The method then returns the complete experience tuple \texttt{(state, action, reward, next\_state, done)}.
\end{enumerate}

The main training loop defined in \texttt{training\_loop\_rdc.py} calls the RDC agent's \texttt{train()} method 

\subsubsection{Training procedure}
The training procedure of the RDC agent is defined by the \texttt{train} method of the \texttt{RegionalDistributionCoordinator}.
The following section describes step-by-step the implementation of this training procedure:

The initial step of the training process starts with sampling experiences from the \texttt{ReplayBuffer}.
As detailed in \autoref{sec:design-rl_framework-rdc}, this replay buffer implements Prioritized Experience Replay (PER).
The sampling is biased towards experiences with high TD-error.
This bias is controlled by the prioritization exponent \texttt{replay\_buffer\_alpha}.
Importance Sampling (IS) weights are used to counteract this bias.
These weights are computed using the \texttt{replay\_buffer\_beta} for controlling the impact of these.
The \texttt{replay\_buffer\_beta} is annealed over the course of \texttt{replay\_buffer\_beta\_frames} training steps to a final value of 1.0.

After sampling experiences from the replay buffer, the Q-values are estimated for each head of the MH-DQN.
The policy network calculates the current Q-values based on the actions $a_c^{RDC}(t)$ taken in the observed state $S_t^{RDC}$.
\begin{equation}\label{eq:ddqn-target}
    Y_t = R_{t+1} + \gamma Q(S_{t+1},\arg\max_{a} Q(S_{t+1},a;\theta_t);\theta_t^-)
\end{equation}

The loss is calculated using the Huber loss, which has been selected due to its robustness \cite{huber_robust_1964}.
The loss is calculated for each head separately, averaged for each sample, and weighted by the IS-weights.
The final loss is the average of these weighted per-sample losses.

The Adam optimizer (\texttt{torch.optim.Adam}) updates the gradients.
Before the optimizer steps, the gradients are clipped to a maximum value of 10 to stabilize the training process.
A learning rate scheduler updates the learning rate linearly every \texttt{lr\_step\_size} by \texttt{lr\_gamma}.

Finally, the agent's parameters are updated accordingly.
Using soft updates with the factor \texttt{tau}, the target network ($\theta^-$) and the policy network ($\theta$), new values for the target network are calculated as follows \cite{hasselt_deep_2016}:
$\theta_{t+1}^- = \tau \theta_{t} + (1-\tau)\theta_t^-$
The \texttt{epsilon} value of the $\epsilon$-greedy strategy is annealed using the following formula:
$\epsilon = \max(\epsilon_{min}, \epsilon_{start} \cdot (\delta_{\epsilon}^{n_{step}}))$
, where $n_{step}$ is the current step count.
Lastly, the priorities of the PER are updated using the newly calculated TD errors by the \texttt{update\_priorities} from the \texttt{ReplayBuffer}.



\subsection{User-Incentive Coordinator}\label{sec:implementation_uic}
The User-Incentive Coordinator (UIC) is implemented using the Proximal Policy Optimization (PPO) implementation from the \texttt{StableBaselines3} library, which is defined in the \texttt{UserIncentiveCoordinator} class.
This class serves as the heart of the second tier of the proposed HRL framework.
The agent 

\begin{table}[H]
    \centering
    \begin{tabularx}{\textwidth}{lX}
        \toprule
        Parameter & Description \\
        \midrule
        \texttt{lr} & The learning rate for the Adam optimizer, which controls the step size of the gradient-based updates to the policy and value function networks. \\
        \texttt{n\_steps} & The number of simulation steps collected from the environment during each rollout phase before a policy update is performed. \\
        \texttt{batch\_size} & The number of training samples used in one minibatch for a single gradient descent step. \\
        \texttt{clip\_range} & The clipping parameter for PPO's surrogate objective function. It defines a small, acceptable range for the policy ratio, preventing excessively large policy updates and ensuring training stability. \\
        \texttt{ent\_coeff} & The coefficient for the entropy bonus term in the loss function. A positive coefficient encourages exploration by penalizing policies that become too deterministic. \\
        \texttt{vf\_coeff} & The coefficient for the value function loss term in the total loss calculation. It balances the updates between the policy (actor) and the value function (critic). \\
        \midrule
        \texttt{gamma} & The discount factor applied to future rewards. It determines the present value of future rewards, influencing how far-sighted the agent's planning horizon is. \\
        \texttt{gae\_lambda} & The lambda parameter for Generalized Advantage Estimation (GAE). It provides a trade-off between bias (low lambda) and variance (high lambda) in the advantage function estimates. \\
        \texttt{target\_kl} & If the Kullback-Leibler (KL) divergence between the new and old policies exceeds this target, updates for the current iteration are stopped. \\
        \midrule
        \texttt{num\_layers} & The number of hidden layers used in the neural network architecture for both the policy (actor) and value (critic) networks. \\
        \texttt{hidden\_size} & The number of neurons within each hidden layer of the policy and value networks. \\
        \texttt{activation\_fn} & The non-linear activation function applied to the output of neurons in the hidden layers. \\
        \midrule
        \texttt{weight\_demand} & The weighting coefficient for the satisfied demand component ($w_{demand}^{UIC}$) in the composite reward function. \\
        \texttt{weight\_cost} & The weighting coefficient for the rebalancing cost penalty component ($w_{cost}^{UIC}$) in the composite reward function. \\
        \texttt{weight\_gini} & The weighting coefficient for the Gini index component ($w_{gini}^{UIC}$) in the composite reward function. \\
        \bottomrule
    \end{tabularx}
    \caption{Configurable hyperparameters for the User-Incentive Coordinator agent.}
    \label{tab:uic_hyperparameters_description}
\end{table}

\subsubsection{State representation}


\subsubsection{Action space representation}

\subsubsection{Reward function representation}

\subsubsection{Environment (\texttt{EScooterUICEnv} Class)}
The \texttt{EScooterUICEnv} class implements the simulation environemnt used for training the UIC agents.
It follows the standard OpenAI Gym interface.
The \texttt{step()} function defines the core logic of the simulation, which is detailed in the following:

\begin{enumerate}
    \item \textbf{Action execution: }
        The \texttt{step()} function gets invoked with the taken action from the UIC as its single parameter.
        This action is a vector containing the incentive levels for each zone corresponding to the community.
    \item \textbf{Rebalancing Simulation: }
        The rebalancing actions are simulated first.
        Therefore, the intended drop-offs from the users are taken and for each drop-off event, the simulation determines whether an incentive is offered.
        This incentive offering is based on analyzing the incentive levels for each neighboring zone.
        If a neighboring zone has a higher incentive level than the intended initially drop-off zone, an incentive is offered.
        If all neighboring zones have a lower incentive level, then no incentive is offered.
        Incentives are only offered for the neighboring cell with the highest incentive level.
        The willingness of the user to take the incentive is modeled by a linear function (\texttt{user\_willingnes\_fn}).
        This linear function multiplies the offered incentive value [0, 1] by the willingness factor of 0.4, resulting in linear willingness between 0 and 0.4.
        This resulting willingness serves as the percentage of taking the incentive.
        As a result, the function returns an updated list of drop-off events, including shifted drop-off events due to incentives, and the number of rebalanced vehicles.
    \item \textbf{Demand Simulation: }
        After all incentives are processed, the demand is simulated.
        Resulting in updated vehicle counts per zone.
    \item \textbf{Observation and Reward Calculation : }
        Lastly, the reward is calculated based on the number of rebalanced vehicles, the satisfied demand, and the distribution of vehicles (see \autoref{sec:design-rl_framework-uic}).
        Then, a new state observation is created an the method returns the complete experience tuple (\texttt{next\_state}, ).
\end{enumerate}
\subsubsection{Training procedure}
The training procedure of the UIC agent is structured as follows:

\begin{enumerate}
    \item \textbf{Experience Collection (Rollout):}
    \item \textbf{Advantage Calculation:}
    \item \textbf{Policy Optimization Loop:}
        The core learning logic is executed within the PPO-specific \texttt{train()} method.
        This method initiates a loop that iterates over the entire RolloutBuffer for a total of \texttt{n\_epochs}.
        In each epoch, the data is shuffled and broken down into minibatches of size \texttt{batch\_size}.
    \item \textbf{Loss Calculation:}
    \item \textbf{Optimization:}
        The total loss is backpropagated to compute the gradients.
        The \texttt{torch.optim.Adam} optimizer, configured with the learning rate \texttt{lr}, then performs a single optimization step to update both the policy and value network parameters ($\theta$).
    \item \textbf{Learning Rate Scheduling:}
        
\end{enumerate}

\subsection{Demand Forecasting Module}\label{sec:implementation_demand-forecasting-module}
The Demand Forecasting Module to feed accurate demand forecasts into the RL agents depends on the implementation by Li et al. \cite{li_improving_2023}.
For training this model, the standard parameters provided by Li et al. have been used.
The demand data had to be prepared for utilization in this model, which is done in the Jupyter Notebook \texttt{prepare\_demand\_forecasting\_data.ipynb}.
A similarity matrix is needed, which defines the kernels for each grid zone.
This similarity matrix is generated by calculating a similarity metric based on the Dynamic Time Warping (DTW) distance (\autoref{eq:irconvlstm-dtw})\cite{li_improving_2023} between the demand of all zones, resulting in a matrix of size $(N_{\mathcal{C}} \times N_{\mathcal{C}})$.
Based on this distance parameter (\texttt{dtw\_dist}), the similarity is calculated (see \autoref{eq:irconvlstm-similarity}).

\begin{equation}\label{eq:irconvlstm-dtw}
\begin{split}
\texttt{dtw\_dist}&(W) = \min \left( \sum_{k=1}^{K} \text{distance}(w_{ki}, w_{kj}) \right)\\
&\text{subject to } 
    \begin{cases}
        W = w_1, w_2, \ldots, w_K \\
        \max(|X|, |Y|) \le K < |X| + |Y| \\
        w_k=(i,j), w_{k+1}=(i',j') \implies i \le i' \le i+1 \text{ and } j \le j' \le j+1
    \end{cases}
\end{split}
\end{equation}

\begin{equation}\label{eq:irconvlstm-similarity}
    \texttt{similarity} = \frac{1}{1 + \texttt{dtw\_dist}}
\end{equation}

A kernel size of 7 has been chosen due to the six neighbors of a hexagon, and adding one for the zone itself.
The cells for the kernel are derived from the similarity matrix calculation.
The 6 most similar zones in terms of the similarity the demand are selected to form the kernel.

For training of the agents and hyperparameter optimization, the forecasts have been pre-computed for the whole training set, which drastically improved computing performances and therefore faster training / HO.

